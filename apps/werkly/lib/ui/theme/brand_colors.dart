import 'package:flutter/material.dart';

/// Locked interim brand tokens (UI-POLISH-HAPPYPATH-V1 · Belkis).
abstract final class BrandColors {
  /// Primary Teal 700.
  static const primary = Color(0xFF0F766E);
  static const onPrimary = Color(0xFFFFFFFF);
  static const primaryContainer = Color(0xFFCCFBF1);

  /// Pro-badge / secondary accent — Indigo 700 (sparsam).
  static const proBadge = Color(0xFF4338CA);
  static const onProBadge = Color(0xFFFFFFFF);
}
